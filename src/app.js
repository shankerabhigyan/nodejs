const uuid = require('uuid'); // import the uuid package 
const {v4 : uuidv4} = require('uuid'); // import the uuid package and rename the v4 function to uuidv4

console.log(uuidv4()); // print the uuid to the console 